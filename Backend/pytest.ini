[pytest]
asyncio_mode = auto
markers =
    integration: Tests that hit real infrastructure boundaries (e.g., Postgres).
    e2e: End-to-end tests across transport, application, and database layers.

pythonpath = src
