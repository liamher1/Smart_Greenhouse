# Allow anonymous connections (No username/password for now)
allow_anonymous true

# Listen on the standard MQTT port for all interfaces
listener 1883 0.0.0.0

# Log settings for debugging
log_dest stdout
log_type all